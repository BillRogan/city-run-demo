class CompetitionType {
  name: string
  distance: string
  icon: string
  background: string

  constructor(name: string, distance: string, icon: string, background: string) {
    this.name = name
    this.distance = distance
    this.icon = icon
    this.background = background
  }
}

export default CompetitionType
