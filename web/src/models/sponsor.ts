class SponsorType {
  name: string
  url: string
  image: string

  constructor(name: string, url: string, image: string) {
    this.name = name
    this.url = url
    this.image = image
  }
}

export default SponsorType
